{
  "buildCommand": "prisma generate && next build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["iad1"],
  "crons": [
    {
      "path": "/api/cron/waitlist-expiry",
      "schedule": "0 6 * * *"
    },
    {
      "path": "/api/cron/redis-keepalive",
      "schedule": "0 8 * * 1"
    }
  ]
}
